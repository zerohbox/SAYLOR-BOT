let handler = async (m, { conn, command, text, usedPrefix }) => {
if (!text) throw `${lenguajeGB['smsAvisoMG']()}
𝙀𝙏𝙄𝙌𝙐𝙀𝙏𝙀 @𝙏𝘼𝙂 𝙊 𝙀𝙎𝘾𝙍𝙄𝘽𝘼 𝙀𝙇 𝙉𝙊𝙈𝘽𝙍𝙀\n𝙏𝘼𝙂 𝙎𝙊𝙈𝙀𝙊𝙉𝙀 @𝙏𝘼𝙂 𝙊𝙍 𝙏𝙔𝙋𝙀 𝙏𝙃𝙀 𝙉𝘼𝙈𝙀` 

if (command == 'gay2') {
let juego = `_*${text.toUpperCase()}* *ES/IS* *${(100).getRandom()}%* *GAY*_ 🏳️‍🌈`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

 // ------------------------------------------------------------------------------------------------------------------------------------------------
  
if (command == 'lesbiana') { 
let juego = `_*${text.toUpperCase()}* *ES/IS* *${(100).getRandom()}%* *${command.replace('how', '').toUpperCase()}*_ 🏳️‍🌈`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
  
// ------------------------------------------------------------------------------------------------------------------------------------------------
  
if (command == 'pajero') {
let juego = `_*${text.toUpperCase()}* *ES/IS* *${(100).getRandom()}%* *${command.replace('how', '').toUpperCase()}*_ 😏💦`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
// ------------------------------------------------------------------------------------------------------------------------------------------------

  if (command == 'virgen') {
  let juego = `_*${text.toUpperCase()}* *ES* *${(100).getRandom()}%* *${command.replace('how', '').toUpperCase()}*_ 😏💦🔥`.trim()
  await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
  
// ------------------------------------------------------------------------------------------------------------------------------------------------

if (command == 'lindo') {
let juego = `_*${text.toUpperCase()}* *ES* *${(100).getRandom()}%* *${command.replace('how', '').toUpperCase()} ->, ¿Es cierto?*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
// ------------------------------------------------------------------------------------------------------------------------------------------------

  if (command == 'linda') {
  let juego = `_*${text.toUpperCase()}* *ES* *${(100).getRandom()}%* *${command.replace('how', '').toUpperCase()} ->, ¿Es cierto?*_`.trim()
  await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

// ------------------------------------------------------------------------------------------------------------------------------------------------
  
if (command == 'pajera') {
let juego = `_*${text.toUpperCase()}* *ES/IS* *${(100).getRandom()}%* *${command.replace('how', '').toUpperCase()}*_ 😏💦`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
  
// ------------------------------------------------------------------------------------------------------------------------------------------------
  
if (command == 'puto') {
let juego = `_*${text.toUpperCase()}* *ES/IS* *${(100).getRandom()}%* *${command.replace('how', '').toUpperCase()},* *MÁS INFORMACIÓN A SU PRIVADO 🔥🥵 XD*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
  
// ------------------------------------------------------------------------------------------------------------------------------------------------
  
if (command == 'puta') {
let juego = `_*${text.toUpperCase()}* *ES/IS* *${(100).getRandom()}%* *${command.replace('how', '').toUpperCase()},* *MÁS INFORMACIÓN A SU PRIVADO 🔥🥵 XD*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

 // ------------------------------------------------------------------------------------------------------------------------------------------------
  
if (command == 'manco') {
let juego = `_*${text.toUpperCase()}* *ES/IS* *${(100).getRandom()}%* *${command.replace('how', '').toUpperCase()} 💩*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
  
// ------------------------------------------------------------------------------------------------------------------------------------------------  
if (command == 'manca') {
let juego = `_*${text.toUpperCase()}* *ES/IS* *${(100).getRandom()}%* *${command.replace('how', '').toUpperCase()} 💩*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
// ------------------------------------------------------------------------------------------------------------------------------------------------ 
  
if (command == 'rata') {
let juego = `_*${text.toUpperCase()}* *ES* *${(100).getRandom()}%* *${command.replace('how', '').toUpperCase()} 🐁 COME QUESO 🧀*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
// ------------------------------------------------------------------------------------------------------------------------------------------------ 

if (command == 'follar') {
let juego = `*🥵💦 @${m.sender.split("@")[0]} Tú te follas A ${text.toUpperCase()} y dice que cojes ${(11).getRandom()}/10 🔥*\n
εつ▄█▀█●`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
// ------------------------------------------------------------------------------------------------------------------------------------------------   
  
if (command == 'prostituto') {
let juego = `_*${text.toUpperCase()}* *ES* *${(100).getRandom()}%* *${command.replace('how', '').toUpperCase()} 🫦👅, QUIEN QUIERE DE SUS SERVICIOS? XD*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
  
// ------------------------------------------------------------------------------------------------------------------------------------------------   
   
if (command == 'prostituta') {
let juego = `_*${text.toUpperCase()}* *ES* *${(100).getRandom()}%* *${command.replace('how', '').toUpperCase()} 🫦👅, QUIEN QUIERE DE SUS SERVICIOS? XD*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
  
// ------------------------------------------------------------------------------------------------------------------------------------------------    

  if (command == 'love') {
      let lovePercentage = (100).getRandom();
      let loveText = '';

      if (lovePercentage <= 10) {
          loveText = '¡Ups! Parece que el amor está en una fase inicial de exploración. 💭 Intenten fortalecer su conexión.';
      } else if (lovePercentage <= 20) {
          loveText = 'Un comienzo modesto, ¡pero cada pequeño paso cuenta! 💖';
      } else if (lovePercentage <= 30) {
          loveText = 'Avanzando en el camino del amor. 🚶‍♂️🚶‍♀️ ¡Cada día más fuerte!';
      } else if (lovePercentage <= 40) {
          loveText = 'El amor crece como un hermoso jardín. 🌷';
      } else if (lovePercentage <= 50) {
          loveText = 'A medio camino, ¡celebremos este amor sólido y constante! 🌟';
      } else if (lovePercentage <= 60) {
          loveText = 'Más allá del 50%, ¡cada día es una nueva aventura juntos! 🌈';
      } else if (lovePercentage <= 70) {
          loveText = 'Superando el 70%, ¡una conexión fuerte y resistente! 💪';
      } else if (lovePercentage <= 80) {
          loveText = 'En el camino del amor, ¡arde brillante como una llama! 🔥';
      } else if (lovePercentage <= 90) {
          loveText = 'Casi al 90%, este amor es una hermosa sinfonía. 🎶';
      } else {
          loveText = '¡Felicidades! Han alcanzado el 100%, un amor perfecto y eterno. 🎉 ¡Que siga creciendo!';
      }

      let juego = `(っ◔◡◔)っ *Medidor de Amor* 💘\n \n*${text.toUpperCase()} y tú se aman un: ${lovePercentage}% ♡*\n
      ░░▄███▄███▄ 
      ░░█████████ 
      ░░▒▀█████▀ 
      ░░▒░░▀█▀*`.trim();

      // Agrega el texto adicional si loveText no está vacío
      if (loveText !== '') {
          juego += `\n\n${loveText}`;
      }

      await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {});
  }


// ------------------------------------------------------------------------------------------------------------------------------------------------
if (command == 'chapar') {
let juego = `*Tú besaste 😙 A ${text.toUpperCase()}* *${(3).getRandom()} veces.*

( ˘ ³˘) 💕`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
// ------------------------------------------------------------------------------------------------------------------------------------------------
if (command == 'golpear') {
let juego = `👊💥(⚈‸☉) *Golpeador 😡*\n\n*Tú golpeaste 🥊 A ${text.toUpperCase()}* *${(5).getRandom()} veces.*
`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
// ------------------------------------------------------------------------------------------------------------------------------------------------
if (command == 'pajear') {
let juego = `*Tú le hiciste ${(2).getRandom()} pajas a ${text.toUpperCase()}*. 🥵💦 `.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
// ------------------------------------------------------------------------------------------------------------------------------------------------
  if (command == 'violin') {
      let porcentaje = (100).getRandom();
      let mensaje = '';

      if (porcentaje >= 80) {
          mensaje = `*Al parecer ${text.toUpperCase()} es ${porcentaje}% VIOLÍN.* 😐\n*¡Llama a la policía inmediatamente! 🚔*`;
      } else if (porcentaje >= 70) {
          mensaje = `*Uh-oh, ${text.toUpperCase()} tiene un ${porcentaje}% de posibilidad de ser VIOLÍN.* 😨\n*¡Mantente alerta!*`;
      } else if (porcentaje >= 60) {
          mensaje = `*${text.toUpperCase()} tiene un ${porcentaje}% de posibilidad de ser VIOLÍN.* 😕\n*Puede ser peligroso, mantente alerta.*`;
      } else if (porcentaje >= 50) {
          mensaje = `*¡Cuidado!* ${text.toUpperCase()} tiene un ${porcentaje}% de posibilidad de ser VIOLÍN.* 😶\n*Quizás deberías tener precaución.*`;
      } else if (porcentaje >= 40) {
          mensaje = `*Hmm... ${text.toUpperCase()} tiene un ${porcentaje}% de posibilidad de ser VIOLÍN.* 😐\n*Quizás deberías tener cuidado.*`;
      } else if (porcentaje >= 30) {
          mensaje = `*¡Alerta! ${text.toUpperCase()} tiene un ${porcentaje}% de posibilidad de ser VIOLÍN.* 😬\n*Puede ser riesgoso.*`;
      } else if (porcentaje >= 20) {
          mensaje = `*¡Ojo con esto!* ${text.toUpperCase()} tiene un ${porcentaje}% de posibilidad de ser VIOLÍN.* 😮\n*Eso suena peligroso.*`;
      } else if (porcentaje >= 10) {
          mensaje = `*Buenas noticias, parece que ${text.toUpperCase()} solo tiene un ${porcentaje}% de ser VIOLÍN.* 🙂\n*Probablemente no sea peligroso.*`;
      } else {
          mensaje = `*¡Atención!* 🚨\n*${text.toUpperCase()} tiene solo un ${porcentaje}% de ser VIOLÍN.* 😅\n*Probablemente no sea peligroso.*`;
      }

      await conn.reply(m.chat, mensaje.trim(), m, m.mentionedJid ? { mentions: m.mentionedJid } : {});
  }


// ------------------------------------------------------------------------------------------------------------------------------------------------

if (command == 'feo') {
let juego = `_*${text.toUpperCase()}* *ES* *${(100).getRandom()}%* *${command.replace('how', '').toUpperCase()} ->, ¿Es cierto?*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
// ------------------------------------------------------------------------------------------------------------------------------------------------

if (command == 'fea') {
let juego = `_*${text.toUpperCase()}* *ES* *${(100).getRandom()}%* *${command.replace('how', '').toUpperCase()} ->, ¿Es cierto?*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

// ------------------------------------------------------------------------------------------------------------------------------------------------

if (command == 'calcular') {
let juego = `*${text.toUpperCase()}* *--> Resultado:* *${(100).getRandom()}%*
`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
// ------------------------------------------------------------------------------------------------------------------------------------------------

if (command == 'calcular2') {
let juego = `*${text.toUpperCase()}* *--> Resultado:* *${(100).getRandom()} CM*
`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
// ------------------------------------------------------------------------------------------------------------------------------------------------

if (command == 'calcular3') {
let juego = `*${text.toUpperCase()}* *--> Resultado:* *${(100).getRandom()}*
`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
  // ------------------------------------------------------------------------------------------------------------------------------------------------
  if (command == 'disparar') {
  const juego = `*Le disparaste ${(100).getRandom()} balas A ${text.toUpperCase()}.*\n
  /̵͇̿̿/’̿’̿ ̿ ̿̿ ̿̿ ̿̿💥
  `.trim()
  await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
//------------------
if (command == 'felizcumple') {
const juego = `
*Heeey ${text.toUpperCase()}* 
*Este pastel es para ti.*

──()──()──()── ()──()───
──▄──▄──▄──▄──▄───
──▓──▓──▓──▓──▓───
─░░░░░░░░░░░░░░░░─
██████████████████
║░✧ Ŧ乇ĿΙZ ĊUMPĿ乇✧ ░║
██████████████████

`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}


// -----------------------------------------------
// ------------------------------------------------------------------------------------------------------------------------------------------------
//conn.sendHydrated(m.chat, juego, wm, null, md, '𝙂𝙖𝙩𝙖𝘽𝙤𝙩-𝙈𝘿', null, null, [
//['𝙈𝙚𝙣𝙪 𝙅𝙪𝙚𝙜𝙤𝙨 | 𝙂𝙖𝙢𝙚𝙨 𝙈𝙚𝙣𝙪 🎡', '#juegosmenu'],
//['𝙊𝙩𝙧𝙖 𝙫𝙚𝙯 | 𝘼𝙜𝙖𝙞𝙣 🤭', `${usedPrefix + command} ${text.toUpperCase()}`],
//['𝙑𝙤𝙡𝙫𝙚𝙧 𝙖𝙡 𝙈𝙚𝙣𝙪́ | 𝘽𝙖𝙘𝙠 𝙩𝙤 𝙈𝙚𝙣𝙪 ☘️', '/menu']
//], m, m.mentionedJid ? {
//mentions: m.mentionedJid
//} : {})} 
}
handler.help = ['felizcumple', 'virgen', 'disparar', 'follar', 'calcular', 'calcular2', 'calcular3', 'fea', 'feo', 'love', 'violin', 'chapar', 'pajear', 'golpear', 'lemide', 'gay2','lindo', 'lesbiana', 'linda', 'pajero','virgen', 'pajera', 'puto', 'puta', 'manco', 'manca', 'rata', 'prostituta', 'prostituto'].map(v => v + ' @tag | nombre')
handler.tags = ['calculator']
handler.command = /^virgen|felizcumple|disparar|follar|calcular3|calcular2|calcular|fea|feo|violin|pajear|love|gay2|lemide|chapar|golpear|lesbiana|pajero|virgen|pajera|puto|puta|manco|manca|rata|prostituta|lindo|linda|prostituto/i
handler.exp = 100
export default handler
